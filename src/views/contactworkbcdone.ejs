<div class="row">
    <div class="col-md-12">
  <% if (messages.success) { %>
      <div class="alert alert-success"><%= messages.success %></div>
  <% } %>
  <% if (messages.uid) %>
    <div class="alert alert-info"><%= messages.uid %></div>
  <% } %>
  <%= console.log(messages.uid) %><
    <div class="card border-warning">
    <div class="card-body">
      <h3 class="card-title">Thank you!</h3>
      <p class="card-text">Your local WorkBC team will be in touch with you soon.</p>
      <p class="card-text">Meanwhile, if you’d like to learn more about the services and trainings offered by WorkBC click <a href="https://www.workbc.ca/Employment-Services.aspx">HERE</a></p>
    </div>
  </div>
  </div>
  </div>